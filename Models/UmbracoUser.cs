using System;

public class UmbracoUser
{
    // Property to store the unique identifier(ConnectionID) of the user
    public string? Id { get; set; }

    // Property to store the name of the user
    public string? Name { get; set; }
}